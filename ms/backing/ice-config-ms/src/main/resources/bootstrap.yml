server:
  port: 8888
spring:
  application:
    name: ice-config-ms
  cloud:
    config:
      server:
        git:
          uri: https://github.com/OElabed/ice-microservices-config.git
          #uri: ${CONFIG_REPO:${HOME}/workspace/ice-microservices-config}
security:
  basic:
    enabled: false
  user:
    name: user
    password: ${CONFIG_SERVICE_PASSWORD}